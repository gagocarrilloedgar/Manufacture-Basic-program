#include<iostrem>

const int MAX=10;

void cercar_codi_barres(Tproducte p[MAX],string Cbarres){
	for (int i=0;i<max;i++){
		if(p[i].codi==Cbarres){
			cout<<...
		}
		else{
			cout<<"el codi no sha trobat "<< endl;
		}
	}
}

void cercar_per_nom(Tproducte p[MAX],string Nomp){
	for (int i=0;i<MAX;i++){
		if(p[i].nom==Nomp){
			cout<<...
		}
		else{
			cout<<"el nom no sha trobat "<< endl;
		}
	}
}

void cercar_fabricant{Tproducte p[MAX], string Fabricantp}{
		for (int i=0;i<MAX;i++){
		if(p[i].fabricant==Fabricantp){
			cout<<...
		}
		else{
			cout<<"el fabricant no sha trobat "<< endl;
		}
	}
}
